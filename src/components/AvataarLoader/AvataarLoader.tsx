import './AvataarLoader.css';

const AvataarLoader = () => {
    return (
        <div id="loader" className="loader">
            <img width="200" alt="avataar loader" src="./loader-avataar.gif" />
        </div>
    );
};

export default AvataarLoader;
